# PHASE_175_ailc_naming_correction WORKLOG

## 判断ログ

### 表示名とパスの扱い

ユーザー指摘に従い、人間向けの表示名は `AiLC` に統一した。

一方、既存データは `docs/consultations/ILC/` 配下に大量の TSV / Excel / PDF が置かれている。フォルダ名を変更するとリンク切れや未追跡データの移動リスクがあるため、今回は表示名のみ修正し、パスは維持した。

### 対象範囲

提案資料、Gensparkプロンプト、相談メモ、INDEX、progress、PHASE_REGISTRY、Phase文書の表示名を修正した。提案資料とGensparkプロンプト内には `ILC` 表記が残っていないことを確認した。

